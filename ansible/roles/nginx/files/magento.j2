upstream fastcgi_backend {
     server  unix:/run/php/php7.2-fpm.sock;
 }

 server {
     listen 80;
     server_name magento.ninjawombat.space;
     set $MAGE_ROOT /var/www/magentoFormacion;
     include /var/www/magentoFormacion/nginx.conf.sample;
 }